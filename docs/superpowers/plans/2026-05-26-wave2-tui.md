# Wave 2 / Frente F — TUI Textual com abas Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TUI interativa com 6 abas (Now / Today / Projects / Models / Tools / Session) navegáveis por atalhos numéricos + setas, alavancando todo o agregador existente. Refresh **somente manual** (tecla `r`); sem auto-polling para evitar flicker e race conditions.

**Architecture:** Pacote `kiro_dash.views` com app Textual minimalista. Cada aba é um widget que computa snapshot ao ser ativada / refrescada e renderiza Rich tables/panels. Widgets reusam funções puras de `aggregator`/`account`/`config`. Comando `kiro-dash tui` lança a app.

**Decisão sobre auto-refresh** (registrada com Léo): historicamente, refresh automático na TUI do `claude-dash` causou flicker e leituras inconsistentes. Esta TUI é **manual-first**: `r` recompoe a aba ativa; um modo opcional de polling pode ser introduzido em Wave 3 com lock e debounce, não agora.

**Tech Stack:** Python 3.12, [`textual >=0.80`](https://textual.textualize.io), Rich (já dependência), pytest.

**Branch:** `feat/wave2-tui`

---

## File Structure

```
src/kiro_dash/views/
├── __init__.py
├── app.py          # KiroDashApp (Textual App)
├── tabs/
│   ├── __init__.py
│   ├── now_tab.py
│   ├── today_tab.py
│   ├── projects_tab.py
│   ├── models_tab.py
│   ├── tools_tab.py
│   └── session_tab.py
└── styles.tcss     # CSS Textual (cores, paddings)
```

| Arquivo | Responsabilidade | Mudança |
|---|---|---|
| `pyproject.toml` | dep textual + entry tui | **Modificar** |
| `src/kiro_dash/views/__init__.py` | Pacote | **Criar** |
| `src/kiro_dash/views/app.py` | App raiz com TabbedContent | **Criar** |
| `src/kiro_dash/views/tabs/now_tab.py` | Widget aba Now | **Criar** |
| `src/kiro_dash/views/tabs/today_tab.py` | Widget aba Today | **Criar** |
| `src/kiro_dash/views/tabs/projects_tab.py` | Aba Projects | **Criar** |
| `src/kiro_dash/views/tabs/models_tab.py` | Aba Models | **Criar** |
| `src/kiro_dash/views/tabs/tools_tab.py` | Aba Tools | **Criar** |
| `src/kiro_dash/views/tabs/session_tab.py` | Aba Session com seleção | **Criar** |
| `src/kiro_dash/views/styles.tcss` | CSS (cores, layout) | **Criar** |
| `src/kiro_dash/cli.py` | Subcomando `tui` | **Modificar** |
| `tests/test_views_tabs.py` | Testes unitários dos snapshots | **Criar** |
| `tests/test_views_app.py` | Smoke da App via App.run_test() | **Criar** |

---

### Task 1: Setup do pacote views + app esqueleto

**Files:**
- Modify: `pyproject.toml`
- Create: `src/kiro_dash/views/__init__.py` (vazio)
- Create: `src/kiro_dash/views/tabs/__init__.py` (vazio)
- Create: `src/kiro_dash/views/styles.tcss`
- Create: `src/kiro_dash/views/app.py`
- Modify: `src/kiro_dash/cli.py` — adicionar subcomando `tui`

- [ ] **Step 1: Adicionar dep e instalar**

Em `pyproject.toml`:

```toml
dependencies = [
    "rich>=13.7",
    "click>=8.1",
    "mcp>=1.0",
    "tomli_w>=1.0",
    "textual>=0.80",
]
```

```bash
cd /home/menzani/Desenvolvimento/mencoding/kiro-dash
source .venv/bin/activate
pip install -q -e ".[dev]"
```

- [ ] **Step 2: Criar pacote vazio**

```bash
mkdir -p src/kiro_dash/views/tabs
touch src/kiro_dash/views/__init__.py
touch src/kiro_dash/views/tabs/__init__.py
```

- [ ] **Step 3: CSS Textual mínimo**

Criar `src/kiro_dash/views/styles.tcss`:

```css
Screen {
    background: $surface;
}

Header {
    background: $primary;
    color: $text;
}

Footer {
    background: $primary-darken-1;
}

TabbedContent {
    height: 1fr;
}

#cycle-banner {
    height: auto;
    padding: 0 1;
    background: $boost;
    color: $text;
}

.warn {
    color: $warning;
}

.danger {
    color: $error;
}
```

- [ ] **Step 4: App esqueleto com 6 abas vazias**

Criar `src/kiro_dash/views/app.py`:

```python
"""TUI Textual do kiro-dash — 6 abas, refresh manual."""
from __future__ import annotations

from pathlib import Path

from textual.app import App, ComposeResult
from textual.binding import Binding
from textual.containers import Container
from textual.widgets import Footer, Header, Static, TabbedContent, TabPane

from kiro_dash import __version__
from kiro_dash.views.tabs.models_tab import ModelsTab
from kiro_dash.views.tabs.now_tab import NowTab
from kiro_dash.views.tabs.projects_tab import ProjectsTab
from kiro_dash.views.tabs.session_tab import SessionTab
from kiro_dash.views.tabs.today_tab import TodayTab
from kiro_dash.views.tabs.tools_tab import ToolsTab


class KiroDashApp(App):
    """App principal."""

    CSS_PATH = "styles.tcss"
    TITLE = f"kiro-dash {__version__}"

    BINDINGS = [
        Binding("1", "show_tab('now')", "Now"),
        Binding("2", "show_tab('today')", "Today"),
        Binding("3", "show_tab('projects')", "Projects"),
        Binding("4", "show_tab('models')", "Models"),
        Binding("5", "show_tab('tools')", "Tools"),
        Binding("6", "show_tab('session')", "Session"),
        Binding("r", "refresh_active", "Refresh"),
        Binding("q", "quit", "Sair"),
        Binding("question_mark", "help", "Ajuda"),
    ]

    def compose(self) -> ComposeResult:
        yield Header(show_clock=True)
        with TabbedContent(initial="now", id="tabs"):
            with TabPane("Now", id="now"):
                yield NowTab()
            with TabPane("Today", id="today"):
                yield TodayTab()
            with TabPane("Projects", id="projects"):
                yield ProjectsTab()
            with TabPane("Models", id="models"):
                yield ModelsTab()
            with TabPane("Tools", id="tools"):
                yield ToolsTab()
            with TabPane("Session", id="session"):
                yield SessionTab()
        yield Footer()

    def action_show_tab(self, tab_id: str) -> None:
        self.query_one(TabbedContent).active = tab_id

    def action_refresh_active(self) -> None:
        active = self.query_one(TabbedContent).active
        pane = self.query_one(f"#{active}", TabPane)
        # Cada tab define um método refresh_snapshot()
        for child in pane.children:
            if hasattr(child, "refresh_snapshot"):
                child.refresh_snapshot()  # type: ignore[attr-defined]

    def action_help(self) -> None:
        self.notify(
            "Atalhos: 1-6 trocar aba; r refresh; q sair",
            title="Ajuda",
            timeout=5,
        )


def run_app() -> int:
    """Entry para o subcomando ``kiro-dash tui``."""
    app = KiroDashApp()
    app.run()
    return 0
```

- [ ] **Step 5: Subcomando `tui` no CLI**

Em `src/kiro_dash/cli.py`, adicionar antes do `if __name__ == "__main__"`:

```python
@main.command()
def tui() -> None:
    """Lança a TUI interativa (6 abas: now/today/projects/models/tools/session)."""
    from kiro_dash.views.app import run_app
    raise SystemExit(run_app())
```

- [ ] **Step 6: Commit (ainda não roda — tabs estão pendentes)**

Não commit ainda; depende dos widgets. Pular pra Task 2.

---

### Task 2: NowTab — sessões ativas (lê parser, sem polling)

**Files:**
- Create: `src/kiro_dash/views/tabs/now_tab.py`
- Create: `tests/test_views_tabs.py`

- [ ] **Step 1: Escrever teste**

Criar `tests/test_views_tabs.py`:

```python
"""Testes dos widgets de aba — funções puras de snapshot."""
from __future__ import annotations

from datetime import datetime, timedelta, timezone
from unittest.mock import patch

from kiro_dash.views.tabs.now_tab import build_now_snapshot
from tests.fixtures.sessions_synthetic import make_session, make_turn


def _two_sessions():
    now = datetime.now(timezone.utc)
    return [
        make_session(
            session_id="aaaa", is_active=True,
            turns=[make_turn(end_timestamp=now - timedelta(minutes=2), credits=3.0)],
        ),
        make_session(
            session_id="bbbb", is_active=False,
            turns=[make_turn(end_timestamp=now - timedelta(hours=2), credits=1.0)],
        ),
    ]


def test_build_now_snapshot_only_active():
    snap = build_now_snapshot(_two_sessions())
    assert snap.active_count == 1
    assert snap.today_credits >= 0
    assert any("aaaa" in row[0] for row in snap.rows)
    assert all("bbbb" not in row[0] for row in snap.rows)


def test_build_now_snapshot_empty_when_no_active():
    snap = build_now_snapshot([
        make_session(session_id="x", is_active=False)
    ])
    assert snap.active_count == 0
    assert snap.rows == []
```

- [ ] **Step 2: Rodar — falha**

```bash
pytest tests/test_views_tabs.py -v
```

Expected: ImportError em `now_tab`.

- [ ] **Step 3: Implementar `NowTab`**

Criar `src/kiro_dash/views/tabs/now_tab.py`:

```python
"""Widget da aba Now — sessões ativas (sem polling automático)."""
from __future__ import annotations

from dataclasses import dataclass, field
from datetime import datetime, timezone

from textual.app import ComposeResult
from textual.containers import Container
from textual.widgets import DataTable, Static

from kiro_dash.aggregator import (
    active_sessions,
    total_credits,
    turns_in_local_day,
)
from kiro_dash.models import Session
from kiro_dash.parser import load_all_sessions


@dataclass(frozen=True, slots=True)
class NowSnapshot:
    active_count: int
    today_credits: float
    timestamp: datetime
    rows: list[tuple[str, ...]] = field(default_factory=list)


def build_now_snapshot(sessions: list[Session]) -> NowSnapshot:
    actives = active_sessions(sessions)
    actives.sort(key=lambda s: s.last_turn_at or s.updated_at, reverse=True)

    today_pairs = turns_in_local_day(sessions)
    today_total = total_credits(today_pairs)

    rows: list[tuple[str, ...]] = []
    for s in actives:
        last = s.last_turn_at or s.updated_at
        delta = (datetime.now(timezone.utc) - last).total_seconds()
        if delta < 60:
            ago = f"{int(delta)}s"
        elif delta < 3600:
            ago = f"{int(delta // 60)}m"
        else:
            ago = f"{int(delta // 3600)}h"
        rows.append((
            s.session_id[:8],
            s.agent_name or "?",
            s.model_id,
            s.cwd or "—",
            str(len(s.turns)),
            f"{s.total_credits:.2f}",
            f"{s.last_context_usage_pct:.1f}",
            ago,
        ))

    return NowSnapshot(
        active_count=len(actives),
        today_credits=today_total,
        timestamp=datetime.now().astimezone(),
        rows=rows,
    )


class NowTab(Container):
    """Aba Now — sessões ativas com refresh manual."""

    def compose(self) -> ComposeResult:
        yield Static(id="now-header")
        yield DataTable(id="now-table", zebra_stripes=True)

    def on_mount(self) -> None:
        table = self.query_one("#now-table", DataTable)
        table.add_columns("sid", "agent", "modelo", "cwd", "turns", "créditos", "ctx %", "último")
        self.refresh_snapshot()

    def refresh_snapshot(self) -> None:
        sessions = load_all_sessions()
        snap = build_now_snapshot(sessions)

        header = self.query_one("#now-header", Static)
        header.update(
            f"[b]{snap.active_count}[/b] sessões ativas — "
            f"hoje: [b green]{snap.today_credits:.2f}[/b green] créditos "
            f"([dim]{snap.timestamp.strftime('%H:%M:%S')}[/dim])"
        )

        table = self.query_one("#now-table", DataTable)
        table.clear()
        for row in snap.rows:
            table.add_row(*row)
```

- [ ] **Step 4: Rodar — passa**

```bash
pytest tests/test_views_tabs.py -v
```

Expected: 2 PASSED.

- [ ] **Step 5: Commit (intermediário; tabs ainda incompletas)**

```bash
git add pyproject.toml src/kiro_dash/views/__init__.py src/kiro_dash/views/tabs/__init__.py src/kiro_dash/views/styles.tcss src/kiro_dash/views/app.py src/kiro_dash/views/tabs/now_tab.py src/kiro_dash/cli.py tests/test_views_tabs.py
git commit -m "feat(views): app Textual + NowTab + subcomando tui"
```

---

### Task 3: TodayTab — agregado do dia

**Files:**
- Create: `src/kiro_dash/views/tabs/today_tab.py`
- Modify: `tests/test_views_tabs.py`

- [ ] **Step 1: Adicionar teste**

Acrescentar em `tests/test_views_tabs.py`:

```python
from kiro_dash.views.tabs.today_tab import build_today_snapshot


def test_build_today_snapshot_aggregates():
    sessions = _two_sessions()
    snap = build_today_snapshot(sessions)
    # by_model deve ter ao menos 1 entrada (claude-opus-4.7 da fixture)
    assert len(snap.by_model) >= 1
    assert snap.total_credits >= 0
```

- [ ] **Step 2: Rodar — falha**

```bash
pytest tests/test_views_tabs.py::test_build_today_snapshot_aggregates -v
```

Expected: ImportError.

- [ ] **Step 3: Implementar**

Criar `src/kiro_dash/views/tabs/today_tab.py`:

```python
"""Aba Today — agregado do dia local."""
from __future__ import annotations

from dataclasses import dataclass, field

from textual.app import ComposeResult
from textual.containers import Container, Horizontal, Vertical
from textual.widgets import DataTable, Static

from kiro_dash.aggregator import (
    Aggregate,
    aggregate_by_agent,
    aggregate_by_cwd,
    aggregate_by_model,
    aggregate_by_session,
    total_credits,
    turns_in_local_day,
)
from kiro_dash.models import Session
from kiro_dash.parser import load_all_sessions


@dataclass(frozen=True, slots=True)
class TodaySnapshot:
    total_credits: float
    total_turns: int
    total_sessions: int
    by_model: list[Aggregate] = field(default_factory=list)
    by_agent: list[Aggregate] = field(default_factory=list)
    by_cwd: list[Aggregate] = field(default_factory=list)
    by_session: list[Aggregate] = field(default_factory=list)


def build_today_snapshot(sessions: list[Session]) -> TodaySnapshot:
    pairs = turns_in_local_day(sessions)
    return TodaySnapshot(
        total_credits=total_credits(pairs),
        total_turns=len(pairs),
        total_sessions=len({s.session_id for s, _ in pairs}),
        by_model=aggregate_by_model(pairs),
        by_agent=aggregate_by_agent(pairs),
        by_cwd=aggregate_by_cwd(pairs),
        by_session=aggregate_by_session(pairs),
    )


def _aggs_to_rows(aggs: list[Aggregate]) -> list[tuple[str, ...]]:
    return [
        (a.label, f"{a.credits:.2f}", str(a.turns), str(a.sessions))
        for a in aggs
    ]


class TodayTab(Container):
    def compose(self) -> ComposeResult:
        yield Static(id="today-header")
        with Horizontal():
            with Vertical():
                yield Static("[b]Por modelo[/b]")
                yield DataTable(id="today-models", zebra_stripes=True)
                yield Static("[b]Por agent[/b]")
                yield DataTable(id="today-agents", zebra_stripes=True)
            with Vertical():
                yield Static("[b]Por projeto (cwd)[/b]")
                yield DataTable(id="today-cwds", zebra_stripes=True)
                yield Static("[b]Por sessão[/b]")
                yield DataTable(id="today-sessions", zebra_stripes=True)

    def on_mount(self) -> None:
        for tid in ("#today-models", "#today-agents", "#today-cwds", "#today-sessions"):
            t = self.query_one(tid, DataTable)
            t.add_columns("label", "créditos", "turns", "sessões")
        self.refresh_snapshot()

    def refresh_snapshot(self) -> None:
        sessions = load_all_sessions()
        snap = build_today_snapshot(sessions)

        self.query_one("#today-header", Static).update(
            f"[b green]{snap.total_credits:.2f}[/b green] créditos  "
            f"[dim]{snap.total_turns} turns / {snap.total_sessions} sessões[/dim]"
        )
        for tid, aggs in [
            ("#today-models", snap.by_model),
            ("#today-agents", snap.by_agent),
            ("#today-cwds", snap.by_cwd),
            ("#today-sessions", snap.by_session),
        ]:
            t = self.query_one(tid, DataTable)
            t.clear()
            for row in _aggs_to_rows(aggs):
                t.add_row(*row)
```

- [ ] **Step 4: Rodar — passa**

```bash
pytest tests/test_views_tabs.py -v
```

Expected: 3 PASSED.

- [ ] **Step 5: Commit**

```bash
git add src/kiro_dash/views/tabs/today_tab.py tests/test_views_tabs.py
git commit -m "feat(views): TodayTab com 4 tabelas de breakdown"
```

---

### Task 4: ProjectsTab + ModelsTab + ToolsTab

**Files:**
- Create: `src/kiro_dash/views/tabs/projects_tab.py`
- Create: `src/kiro_dash/views/tabs/models_tab.py`
- Create: `src/kiro_dash/views/tabs/tools_tab.py`
- Modify: `tests/test_views_tabs.py`

- [ ] **Step 1: Adicionar testes**

Acrescentar em `tests/test_views_tabs.py`:

```python
from kiro_dash.views.tabs.models_tab import build_models_snapshot
from kiro_dash.views.tabs.projects_tab import build_projects_snapshot


def test_build_projects_snapshot_uses_window():
    sessions = _two_sessions()
    snap = build_projects_snapshot(sessions, days=7)
    assert snap.window_days == 7
    # Aggs ordenado por créditos desc
    if len(snap.aggs) >= 2:
        assert snap.aggs[0].credits >= snap.aggs[1].credits


def test_build_models_snapshot_uses_window():
    sessions = _two_sessions()
    snap = build_models_snapshot(sessions, days=30)
    assert snap.window_days == 30
```

(Tools tab é testada via `tools_command` da Wave 1; aqui faz smoke da função do widget.)

- [ ] **Step 2: Rodar — falha**

```bash
pytest tests/test_views_tabs.py -v
```

Expected: 2 novos failes.

- [ ] **Step 3: Implementar `projects_tab.py`**

```python
"""Aba Projects — top projetos por créditos numa janela."""
from __future__ import annotations

from dataclasses import dataclass, field

from textual.app import ComposeResult
from textual.containers import Container
from textual.widgets import DataTable, Static

from kiro_dash.aggregator import (
    Aggregate,
    aggregate_by_cwd,
    turns_in_last_days,
)
from kiro_dash.models import Session
from kiro_dash.parser import load_all_sessions


@dataclass(frozen=True, slots=True)
class ProjectsSnapshot:
    window_days: int
    aggs: list[Aggregate] = field(default_factory=list)


def build_projects_snapshot(sessions: list[Session], *, days: int = 7) -> ProjectsSnapshot:
    pairs = turns_in_last_days(sessions, days=days)
    return ProjectsSnapshot(window_days=days, aggs=aggregate_by_cwd(pairs))


class ProjectsTab(Container):
    DEFAULT_DAYS = 7

    def compose(self) -> ComposeResult:
        yield Static(id="projects-header")
        yield DataTable(id="projects-table", zebra_stripes=True)

    def on_mount(self) -> None:
        t = self.query_one("#projects-table", DataTable)
        t.add_columns("projeto (cwd)", "créditos", "turns", "sessões", "duração")
        self.refresh_snapshot()

    def refresh_snapshot(self) -> None:
        sessions = load_all_sessions()
        snap = build_projects_snapshot(sessions, days=self.DEFAULT_DAYS)
        self.query_one("#projects-header", Static).update(
            f"[b]Projetos[/b] — últimos {snap.window_days}d"
        )
        t = self.query_one("#projects-table", DataTable)
        t.clear()
        for a in snap.aggs:
            dur = int(a.duration.total_seconds())
            dur_str = f"{dur // 60}m" if dur < 3600 else f"{dur // 3600}h{(dur % 3600) // 60:02d}m"
            t.add_row(a.label, f"{a.credits:.2f}", str(a.turns), str(a.sessions), dur_str)
```

- [ ] **Step 4: Implementar `models_tab.py`**

(Estrutura idêntica, trocando `aggregate_by_cwd` → `aggregate_by_model` e label "modelo".)

```python
"""Aba Models — top modelos por créditos numa janela."""
from __future__ import annotations

from dataclasses import dataclass, field

from textual.app import ComposeResult
from textual.containers import Container
from textual.widgets import DataTable, Static

from kiro_dash.aggregator import Aggregate, aggregate_by_model, turns_in_last_days
from kiro_dash.models import Session
from kiro_dash.parser import load_all_sessions


@dataclass(frozen=True, slots=True)
class ModelsSnapshot:
    window_days: int
    aggs: list[Aggregate] = field(default_factory=list)


def build_models_snapshot(sessions: list[Session], *, days: int = 7) -> ModelsSnapshot:
    pairs = turns_in_last_days(sessions, days=days)
    return ModelsSnapshot(window_days=days, aggs=aggregate_by_model(pairs))


class ModelsTab(Container):
    DEFAULT_DAYS = 7

    def compose(self) -> ComposeResult:
        yield Static(id="models-header")
        yield DataTable(id="models-table", zebra_stripes=True)

    def on_mount(self) -> None:
        t = self.query_one("#models-table", DataTable)
        t.add_columns("modelo", "créditos", "turns", "sessões", "duração")
        self.refresh_snapshot()

    def refresh_snapshot(self) -> None:
        sessions = load_all_sessions()
        snap = build_models_snapshot(sessions, days=self.DEFAULT_DAYS)
        self.query_one("#models-header", Static).update(
            f"[b]Modelos[/b] — últimos {snap.window_days}d"
        )
        t = self.query_one("#models-table", DataTable)
        t.clear()
        for a in snap.aggs:
            dur = int(a.duration.total_seconds())
            dur_str = f"{dur // 60}m" if dur < 3600 else f"{dur // 3600}h{(dur % 3600) // 60:02d}m"
            t.add_row(a.label, f"{a.credits:.2f}", str(a.turns), str(a.sessions), dur_str)
```

- [ ] **Step 5: Implementar `tools_tab.py`**

```python
"""Aba Tools — breakdown de tool calls nas últimas N horas."""
from __future__ import annotations

from textual.app import ComposeResult
from textual.containers import Container
from textual.widgets import DataTable, Static

from kiro_dash.aggregator import aggregate_tools_in_window
from kiro_dash.parser import DEFAULT_SESSIONS_DIR


class ToolsTab(Container):
    DEFAULT_HOURS = 24

    def compose(self) -> ComposeResult:
        yield Static(id="tools-header")
        yield DataTable(id="tools-table", zebra_stripes=True)

    def on_mount(self) -> None:
        t = self.query_one("#tools-table", DataTable)
        t.add_columns("tool", "count", "sessões", "erros")
        self.refresh_snapshot()

    def refresh_snapshot(self) -> None:
        aggs = aggregate_tools_in_window(DEFAULT_SESSIONS_DIR, hours=self.DEFAULT_HOURS)
        total = sum(a["count"] for a in aggs)
        err_total = sum(a["errors"] for a in aggs)
        self.query_one("#tools-header", Static).update(
            f"[b]Tools[/b] — últimas {self.DEFAULT_HOURS}h  "
            f"[cyan]{total} chamadas[/cyan]  "
            + (f"[red]{err_total} erros[/red]" if err_total else "")
        )
        t = self.query_one("#tools-table", DataTable)
        t.clear()
        for a in aggs:
            err_text = f"[red]{a['errors']}[/red]" if a["errors"] else "[dim]0[/dim]"
            t.add_row(a["name"], str(a["count"]), str(a["sessions"]), err_text)
```

- [ ] **Step 6: Rodar — passa**

```bash
pytest tests/test_views_tabs.py -v
```

Expected: 5 PASSED.

- [ ] **Step 7: Commit**

```bash
git add src/kiro_dash/views/tabs/projects_tab.py src/kiro_dash/views/tabs/models_tab.py src/kiro_dash/views/tabs/tools_tab.py tests/test_views_tabs.py
git commit -m "feat(views): ProjectsTab + ModelsTab + ToolsTab"
```

---

### Task 5: SessionTab com seleção (↑/↓ + Enter)

**Files:**
- Create: `src/kiro_dash/views/tabs/session_tab.py`
- Modify: `tests/test_views_tabs.py`

- [ ] **Step 1: Implementar `SessionTab`**

```python
"""Aba Session — lista todas as sessões; Enter abre drill-down inline."""
from __future__ import annotations

from textual.app import ComposeResult
from textual.containers import Container, Vertical
from textual.widgets import DataTable, Static

from kiro_dash.parser import discover_sessions, load_session_file


class SessionTab(Container):
    def compose(self) -> ComposeResult:
        yield Static(id="session-header")
        with Vertical():
            yield DataTable(id="session-list", zebra_stripes=True, cursor_type="row")
            yield Static(id="session-details")

    def on_mount(self) -> None:
        t = self.query_one("#session-list", DataTable)
        t.add_columns("sid", "agent", "modelo", "turns", "créditos", "atualizada")
        self.refresh_snapshot()

    def refresh_snapshot(self) -> None:
        paths = discover_sessions()
        sessions = []
        for p in paths:
            s = load_session_file(p)
            if s is not None:
                sessions.append(s)

        self.query_one("#session-header", Static).update(
            f"[b]{len(sessions)}[/b] sessões — selecione com ↑/↓, Enter para drill-down"
        )
        t = self.query_one("#session-list", DataTable)
        t.clear()
        self._sessions_by_index = sessions
        for s in sessions:
            sid = f"{s.session_id[:8]}{' ●' if s.is_active else ''}"
            t.add_row(
                sid,
                s.agent_name or "?",
                s.model_id,
                str(len(s.turns)),
                f"{s.total_credits:.2f}",
                s.updated_at.astimezone().strftime("%Y-%m-%d %H:%M"),
            )
        self.query_one("#session-details", Static).update("")

    def on_data_table_row_selected(self, event) -> None:  # type: ignore[no-untyped-def]
        idx = event.cursor_row
        if not (0 <= idx < len(self._sessions_by_index)):
            return
        s = self._sessions_by_index[idx]
        lines = [
            f"[b]{s.session_id}[/b]",
            f"título: {s.title or '—'}",
            f"agent: {s.agent_name}  modelo: {s.model_id} (×{s.rate_multiplier})",
            f"cwd: {s.cwd}",
            f"turns: {len(s.turns)}  créditos: {s.total_credits:.2f}  ctx final: {s.last_context_usage_pct:.1f}%",
            f"criada: {s.created_at.astimezone().isoformat(timespec='seconds')}",
            f"atualizada: {s.updated_at.astimezone().isoformat(timespec='seconds')}",
        ]
        self.query_one("#session-details", Static).update("\n".join(lines))
```

- [ ] **Step 2: Adicionar teste do builder (sem App.run_test)**

Acrescentar em `tests/test_views_tabs.py`:

```python
from kiro_dash.views.tabs.session_tab import SessionTab  # noqa: F401 — sanity import


def test_session_tab_module_imports_cleanly():
    """Smoke — o módulo importa sem erro."""
    from kiro_dash.views.tabs import session_tab
    assert hasattr(session_tab, "SessionTab")
```

- [ ] **Step 3: Rodar — passa**

```bash
pytest tests/test_views_tabs.py -v
```

Expected: 6 PASSED.

- [ ] **Step 4: Commit**

```bash
git add src/kiro_dash/views/tabs/session_tab.py tests/test_views_tabs.py
git commit -m "feat(views): SessionTab com seleção e drill-down inline"
```

---

### Task 6: Smoke da App via `App.run_test()`

**Files:**
- Create: `tests/test_views_app.py`

- [ ] **Step 1: Escrever teste headless**

Criar `tests/test_views_app.py`:

```python
"""Smoke test da KiroDashApp — usa App.run_test() do Textual."""
from __future__ import annotations

import pytest
from textual.widgets import TabbedContent

from kiro_dash.views.app import KiroDashApp


@pytest.mark.asyncio
async def test_app_starts_with_now_active():
    app = KiroDashApp()
    async with app.run_test() as pilot:
        tabbed = pilot.app.query_one(TabbedContent)
        assert tabbed.active == "now"


@pytest.mark.asyncio
async def test_keys_switch_tabs():
    app = KiroDashApp()
    async with app.run_test() as pilot:
        await pilot.press("2")
        await pilot.pause()
        tabbed = pilot.app.query_one(TabbedContent)
        assert tabbed.active == "today"
        await pilot.press("4")
        await pilot.pause()
        assert tabbed.active == "models"


@pytest.mark.asyncio
async def test_r_does_not_crash_on_each_tab():
    app = KiroDashApp()
    async with app.run_test() as pilot:
        for tab_key in ("1", "2", "3", "4", "5", "6"):
            await pilot.press(tab_key)
            await pilot.pause()
            await pilot.press("r")
            await pilot.pause()
        # Se chegou aqui sem exceção, refresh em cada aba está saudável.
```

- [ ] **Step 2: Adicionar dep de teste async**

```bash
pip install -q pytest-asyncio
```

E adicionar em `pyproject.toml` em `optional-dependencies.dev`:

```toml
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
]
```

E em `[tool.pytest.ini_options]`:

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
asyncio_mode = "auto"
```

- [ ] **Step 3: Rodar — passa**

```bash
pip install -q -e ".[dev]"
pytest tests/test_views_app.py -v
```

Expected: 3 PASSED.

- [ ] **Step 4: Smoke manual interativo**

Em outro terminal:

```bash
kiro-dash tui
```

Expected: TUI abre na aba Now mostrando sessões ativas; teclas 1-6 trocam aba; `r` redesenha a aba; `q` sai limpo. **Nenhum redesenho automático.**

- [ ] **Step 5: Commit final**

```bash
git add tests/test_views_app.py pyproject.toml
git commit -m "test(views): smoke da App + bindings + refresh por aba"
```

---

### Task 7: README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Adicionar seção da TUI**

Antes da Licença:

```markdown
## TUI interativa

```bash
kiro-dash tui
```

Atalhos:

| Tecla | Ação |
|---|---|
| `1` | Aba Now (sessões ativas) |
| `2` | Aba Today |
| `3` | Aba Projects |
| `4` | Aba Models |
| `5` | Aba Tools |
| `6` | Aba Session (com seleção e drill-down) |
| `←` / `→` | Navegar abas (bind padrão do Textual) |
| `r` | Refresh manual da aba ativa |
| `?` | Ajuda |
| `q` | Sair |

**Sem auto-refresh** — leituras inconsistentes do passado fizeram este
design ser intencionalmente conservador. Se precisar de polling, abrir
sessão TUI separada e usar `r` no ritmo desejado.
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs(tui): atalhos e decisão de não-auto-refresh"
```

---

## Self-Review Checklist

- [ ] Sem `set_interval`, `auto_refresh`, ou polling em qualquer widget
- [ ] Cada aba expõe `refresh_snapshot()` (recompoe sob demanda)
- [ ] `r` invoca `refresh_snapshot` da aba ativa
- [ ] App.run_test() valida bindings e refresh sem crash
- [ ] CSS minimal, sem dependências externas
- [ ] Funções `build_*_snapshot` são puras e testáveis sem subir App
- [ ] README documenta a decisão de manual-only

## Done When

- `pytest tests/test_views_tabs.py tests/test_views_app.py -v` → 9 PASSED
- `kiro-dash tui` interativo: troca de aba responsiva, `r` recompõe, `q` sai limpo
- 7 commits no branch `feat/wave2-tui`
