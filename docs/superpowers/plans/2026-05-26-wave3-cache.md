# Wave 3 / Frente I — Cache parser incremental Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Acelerar leituras repetidas do parser introduzindo cache mtime-based em `~/.cache/kiro-dash/`. Sessions e tools de `.json`/`.jsonl` ficam memoizados por `(path, mtime, size)`. Comandos novos `cache info` e `cache clear` para inspeção/limpeza.

**Architecture:**

- Módulo `kiro_dash.cache` com API: `get_cached(path) -> obj | None`, `put_cached(path, obj)`, `clear()`, `info()`.
- Cache key: `(canonical_path, mtime_ns, size)`. Qualquer mudança no arquivo invalida.
- Storage: arquivos JSON pickle-friendly em `~/.cache/kiro-dash/sessions/<sha256(path)>.json` e `~/.cache/kiro-dash/jsonl/<sha256(path)>.json`. SHA do path evita collisions e nomes longos.
- Integração:
  - `parser.load_session_file` → consulta cache antes do `json.load`; se hit válido, devolve dataclass reconstruída.
  - `jsonl_parser.parse_jsonl_tools` → mesmo padrão.
- TTL implícito: cache só vive enquanto mtime/size do source casam. Sessões em curso (cwd com `.lock`) são **bypassadas** do cache (sempre relê).
- Override por env: `KIRO_DASH_NO_CACHE=1` desliga.

**Tech Stack:** Python 3.12 stdlib (json, hashlib, dataclasses, pickle opcional).

**Branch:** `feat/wave3-cache`

---

## File Structure

| Arquivo | Responsabilidade | Mudança |
|---|---|---|
| `src/kiro_dash/cache.py` | Cache layer com mtime invalidation | **Criar** |
| `src/kiro_dash/parser.py` | Hook em `load_session_file` (bypass se ativa) | **Modificar** |
| `src/kiro_dash/jsonl_parser.py` | Hook em `parse_jsonl_tools` | **Modificar** |
| `src/kiro_dash/cli.py` | Subgrupo `cache info/clear` | **Modificar** |
| `tests/test_cache.py` | Cobertura do cache layer | **Criar** |
| `tests/test_cache_integration.py` | Integração parser ↔ cache | **Criar** |

---

### Task 1: Cache layer puro

**Files:**
- Create: `src/kiro_dash/cache.py`
- Create: `tests/test_cache.py`

- [ ] **Step 1: Escrever testes**

Criar `tests/test_cache.py`:

```python
"""Cobertura do cache layer com mtime invalidation."""
from __future__ import annotations

import json
import os
import time
from pathlib import Path

import pytest

from kiro_dash.cache import (
    SessionsCache,
    cache_dir_default,
    clear_cache,
    cache_info,
)


@pytest.fixture
def cache(tmp_path):
    return SessionsCache(root=tmp_path / "cache")


@pytest.fixture
def src_file(tmp_path):
    p = tmp_path / "session.json"
    p.write_text(json.dumps({"k": "v"}))
    return p


def test_get_returns_none_when_empty(cache, src_file):
    assert cache.get(src_file) is None


def test_put_then_get_hits(cache, src_file):
    cache.put(src_file, {"data": [1, 2, 3]})
    assert cache.get(src_file) == {"data": [1, 2, 3]}


def test_get_misses_after_mtime_change(cache, src_file):
    cache.put(src_file, {"data": "old"})
    time.sleep(0.01)
    src_file.write_text(json.dumps({"k": "v2"}))
    assert cache.get(src_file) is None


def test_get_misses_after_size_change(cache, src_file):
    cache.put(src_file, {"data": "old"})
    # Truque para mudar size sem mudar mtime: usa os.utime depois de write
    mtime = src_file.stat().st_mtime
    src_file.write_text(json.dumps({"k": "v" * 100}))
    os.utime(src_file, (mtime, mtime))
    assert cache.get(src_file) is None


def test_get_returns_none_when_source_deleted(cache, src_file):
    cache.put(src_file, {"data": "x"})
    src_file.unlink()
    assert cache.get(src_file) is None


def test_clear_remove_all(cache, src_file):
    cache.put(src_file, {"data": "x"})
    cache.clear()
    assert cache.get(src_file) is None


def test_info_reports_count_and_size(cache, src_file, tmp_path):
    other = tmp_path / "other.json"
    other.write_text("{}")
    cache.put(src_file, {"a": 1})
    cache.put(other, {"b": 2})
    info = cache.info()
    assert info["entries"] == 2
    assert info["bytes"] > 0


def test_no_cache_env_disables(cache, src_file, monkeypatch):
    monkeypatch.setenv("KIRO_DASH_NO_CACHE", "1")
    cache.put(src_file, {"x": 1})
    assert cache.get(src_file) is None


def test_cache_dir_default_uses_xdg(monkeypatch, tmp_path):
    monkeypatch.setenv("XDG_CACHE_HOME", str(tmp_path))
    assert cache_dir_default() == tmp_path / "kiro-dash"
```

- [ ] **Step 2: Rodar — falha (sem módulo)**

```bash
cd /home/menzani/Desenvolvimento/mencoding/kiro-dash
source .venv/bin/activate
pytest tests/test_cache.py -v
```

- [ ] **Step 3: Implementar `cache.py`**

Criar `src/kiro_dash/cache.py`:

```python
"""Cache de leitura para parser, mtime/size-invalidated.

Cache em ``~/.cache/kiro-dash/`` (ou ``$XDG_CACHE_HOME/kiro-dash/``).
Cada entrada armazena ``{path, mtime_ns, size, payload}``. Hit válido
exige match exato dos 3 primeiros.

Bypass via ``KIRO_DASH_NO_CACHE=1`` no env.
"""
from __future__ import annotations

import hashlib
import json
import os
from dataclasses import dataclass
from pathlib import Path
from typing import Any


def cache_dir_default() -> Path:
    """Diretório de cache padrão respeitando XDG_CACHE_HOME."""
    base = os.environ.get("XDG_CACHE_HOME") or str(Path.home() / ".cache")
    return Path(base) / "kiro-dash"


def _key_for(path: Path) -> str:
    """SHA256 do path canônico — nome curto sem colisão."""
    canon = str(path.resolve())
    return hashlib.sha256(canon.encode()).hexdigest()


def _disabled() -> bool:
    return os.environ.get("KIRO_DASH_NO_CACHE", "") == "1"


@dataclass(slots=True)
class SessionsCache:
    """Cache key = (path, mtime_ns, size). Invalida em qualquer mudança."""

    root: Path

    def __post_init__(self) -> None:
        self.root.mkdir(parents=True, exist_ok=True)

    def _entry_path(self, src: Path) -> Path:
        return self.root / f"{_key_for(src)}.json"

    def get(self, src: Path) -> Any | None:
        if _disabled():
            return None
        if not src.exists():
            return None
        ep = self._entry_path(src)
        if not ep.exists():
            return None
        try:
            with open(ep) as f:
                entry = json.load(f)
        except (OSError, json.JSONDecodeError):
            return None
        st = src.stat()
        if (
            entry.get("path") != str(src.resolve())
            or entry.get("mtime_ns") != st.st_mtime_ns
            or entry.get("size") != st.st_size
        ):
            return None
        return entry.get("payload")

    def put(self, src: Path, payload: Any) -> None:
        if _disabled():
            return
        if not src.exists():
            return
        st = src.stat()
        entry = {
            "path": str(src.resolve()),
            "mtime_ns": st.st_mtime_ns,
            "size": st.st_size,
            "payload": payload,
        }
        ep = self._entry_path(src)
        try:
            with open(ep, "w") as f:
                json.dump(entry, f)
        except OSError:
            pass  # cache é best-effort, nunca falha a chamada

    def clear(self) -> int:
        """Apaga todo cache. Retorna nº de entradas removidas."""
        n = 0
        if not self.root.exists():
            return 0
        for p in self.root.glob("*.json"):
            try:
                p.unlink()
                n += 1
            except OSError:
                pass
        return n

    def info(self) -> dict[str, int]:
        """Conta de entradas e bytes totais."""
        n, total = 0, 0
        if not self.root.exists():
            return {"entries": 0, "bytes": 0}
        for p in self.root.glob("*.json"):
            try:
                total += p.stat().st_size
                n += 1
            except OSError:
                pass
        return {"entries": n, "bytes": total}


# ─── singletons / helpers ─────────────────────────────────────────────────


_sessions_cache: SessionsCache | None = None
_jsonl_cache: SessionsCache | None = None


def sessions_cache() -> SessionsCache:
    global _sessions_cache
    if _sessions_cache is None:
        _sessions_cache = SessionsCache(root=cache_dir_default() / "sessions")
    return _sessions_cache


def jsonl_cache() -> SessionsCache:
    global _jsonl_cache
    if _jsonl_cache is None:
        _jsonl_cache = SessionsCache(root=cache_dir_default() / "jsonl")
    return _jsonl_cache


def clear_cache() -> dict[str, int]:
    """Limpa ambos os caches. Retorna {sessions: N, jsonl: M}."""
    return {
        "sessions": sessions_cache().clear(),
        "jsonl": jsonl_cache().clear(),
    }


def cache_info() -> dict[str, dict[str, int]]:
    """Inspeção dos caches sessions/jsonl."""
    return {
        "sessions": sessions_cache().info(),
        "jsonl": jsonl_cache().info(),
    }
```

- [ ] **Step 4: Rodar — passa**

```bash
pytest tests/test_cache.py -v
```

Expected: 9 PASSED.

- [ ] **Step 5: Commit**

```bash
git add src/kiro_dash/cache.py tests/test_cache.py
git -c user.email='leonardo.menzani@gmail.com' -c user.name='mencoding' \
  commit -m "feat(cache): SessionsCache mtime/size-invalidated com bypass via env"
```

---

### Task 2: Hook em `parser.load_session_file` (bypassa sessões ativas)

**Files:**
- Modify: `src/kiro_dash/parser.py`
- Create: `tests/test_cache_integration.py`

- [ ] **Step 1: Escrever teste de integração**

Criar `tests/test_cache_integration.py`:

```python
"""Integração parser ↔ cache."""
from __future__ import annotations

import json
import time
from pathlib import Path
from unittest.mock import patch

import pytest

from kiro_dash.cache import sessions_cache
from kiro_dash.parser import load_session_file


@pytest.fixture
def isolated_cache(tmp_path, monkeypatch):
    """Substitui o singleton de cache por um sob tmp_path."""
    monkeypatch.setenv("XDG_CACHE_HOME", str(tmp_path / "cache"))
    # Resetar singleton para reler env
    import kiro_dash.cache as cmod
    cmod._sessions_cache = None
    cmod._jsonl_cache = None
    yield
    cmod._sessions_cache = None
    cmod._jsonl_cache = None


def _write_session(path: Path) -> None:
    payload = {
        "session_id": str(path.stem),
        "title": "x",
        "cwd": "/tmp",
        "created_at": "2026-05-26T00:00:00Z",
        "updated_at": "2026-05-26T00:00:00Z",
        "session_state": {
            "version": "1",
            "agent_name": "nyx",
            "rts_model_state": {
                "model_info": {
                    "model_id": "claude-opus-4.7",
                    "rate_multiplier": 5.0,
                    "context_window_tokens": 200000,
                    "rate_unit": "credit",
                }
            },
            "conversation_metadata": {"user_turn_metadatas": []},
        },
    }
    path.write_text(json.dumps(payload))


def test_second_load_hits_cache(tmp_path, isolated_cache):
    p = tmp_path / "abc.json"
    _write_session(p)

    # 1ª leitura — popula cache
    s1 = load_session_file(p)
    assert s1 is not None

    # 2ª leitura — deve vir do cache (verifica via mock no json.load)
    with patch("kiro_dash.parser.json.load") as mock_load:
        s2 = load_session_file(p)
        # Cache hit: json.load não foi chamado
        assert s2 is not None
        mock_load.assert_not_called()


def test_cache_invalidates_after_mtime_change(tmp_path, isolated_cache):
    p = tmp_path / "abc.json"
    _write_session(p)

    s1 = load_session_file(p)
    time.sleep(0.01)
    p.write_text(p.read_text())  # touch

    with patch("kiro_dash.parser.json.load", wraps=__import__("json").load) as mock_load:
        load_session_file(p)
        mock_load.assert_called_once()  # mtime mudou, releu


def test_active_session_bypasses_cache(tmp_path, isolated_cache):
    p = tmp_path / "abc.json"
    _write_session(p)
    lock = p.with_suffix(".lock")
    lock.write_text('{"pid":1,"started_at":"2026-05-26T00:00:00Z"}')

    load_session_file(p)  # popula

    # Mesmo sem alterar arquivo, sessão ativa força releitura
    with patch("kiro_dash.parser.json.load", wraps=__import__("json").load) as mock_load:
        load_session_file(p)
        mock_load.assert_called_once()
```

- [ ] **Step 2: Rodar — falha**

```bash
pytest tests/test_cache_integration.py -v
```

- [ ] **Step 3: Implementar hook em `parser.py`**

No topo:

```python
from kiro_dash.cache import sessions_cache
```

Em `load_session_file`, no início (depois das checagens iniciais e antes do `with open`):

```python
def load_session_file(path: Path) -> Session | None:
    """Carrega Session de path. Retorna None se inválido.

    Cache mtime-based em ``~/.cache/kiro-dash/sessions/``. Sessões com
    ``.lock`` correspondente (ativas) bypassam o cache para garantir
    leitura sempre fresca.
    """
    if not path.exists() or not path.is_file():
        return None

    lock_path = path.with_suffix(".lock")
    is_active = lock_path.exists()

    if not is_active:
        cached = sessions_cache().get(path)
        if cached is not None:
            try:
                return _session_from_payload(cached)
            except (KeyError, ValueError, TypeError):
                pass  # cache corrompido, cai para releitura

    try:
        with open(path) as f:
            data = json.load(f)
    except (OSError, json.JSONDecodeError):
        return None

    session = _parse_session_data(data, path)
    if session is None:
        return None

    if not is_active:
        sessions_cache().put(path, _session_to_payload(session))

    return session


def _session_to_payload(session: Session) -> dict:
    """Converte ``Session`` para dict serializável (round-trip cache)."""
    return {
        "session_id": session.session_id,
        "title": session.title,
        "cwd": session.cwd,
        "created_at": session.created_at.isoformat(),
        "updated_at": session.updated_at.isoformat(),
        "agent_name": session.agent_name,
        "model_id": session.model_id,
        "rate_multiplier": session.rate_multiplier,
        "context_window_tokens": session.context_window_tokens,
        "is_active": session.is_active,
        "turns": [
            {
                "agent_id": t.agent_id,
                "parent_agent_id": t.parent_agent_id,
                "end_timestamp": t.end_timestamp.isoformat() if t.end_timestamp else None,
                "duration_secs": t.duration_secs,
                "duration_nanos": t.duration_nanos,
                "end_reason": t.end_reason,
                "builtin_tool_uses": t.builtin_tool_uses,
                "number_of_cycles": t.number_of_cycles,
                "context_usage_pct": t.context_usage_pct,
                "credits": t.credits,
            }
            for t in session.turns
        ],
    }


def _session_from_payload(payload: dict) -> Session:
    """Reconstrói ``Session`` a partir do dict do cache."""
    from datetime import datetime
    turns = [
        Turn(
            agent_id=t["agent_id"],
            parent_agent_id=t.get("parent_agent_id"),
            end_timestamp=(
                datetime.fromisoformat(t["end_timestamp"])
                if t["end_timestamp"]
                else None
            ),
            duration_secs=t["duration_secs"],
            duration_nanos=t["duration_nanos"],
            end_reason=t["end_reason"],
            builtin_tool_uses=t.get("builtin_tool_uses", 0),
            number_of_cycles=t.get("number_of_cycles", 0),
            context_usage_pct=t["context_usage_pct"],
            credits=t["credits"],
        )
        for t in payload["turns"]
    ]
    return Session(
        session_id=payload["session_id"],
        title=payload["title"],
        cwd=payload["cwd"],
        created_at=datetime.fromisoformat(payload["created_at"]),
        updated_at=datetime.fromisoformat(payload["updated_at"]),
        agent_name=payload["agent_name"],
        model_id=payload["model_id"],
        rate_multiplier=payload["rate_multiplier"],
        context_window_tokens=payload["context_window_tokens"],
        is_active=payload["is_active"],
        turns=turns,
    )
```

> **Atenção:** se a função atual já trata o body do `with open` inline, refatore para extrair `_parse_session_data(data, path)` antes (mantendo testes verdes da Wave 1/2). O propósito da extração é só permitir o reuso na branch cache-miss.

- [ ] **Step 4: Rodar testes**

```bash
pytest tests/test_cache_integration.py tests/test_parser*.py -v 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
git add src/kiro_dash/parser.py tests/test_cache_integration.py
git -c user.email='leonardo.menzani@gmail.com' -c user.name='mencoding' \
  commit -m "feat(parser): cache em load_session_file (bypass sessões ativas)"
```

---

### Task 3: Hook em `jsonl_parser.parse_jsonl_tools`

**Files:**
- Modify: `src/kiro_dash/jsonl_parser.py`
- Modify: `tests/test_cache_integration.py`

- [ ] **Step 1: Adicionar teste**

Acrescentar em `tests/test_cache_integration.py`:

```python
def test_jsonl_parser_uses_cache(tmp_path, isolated_cache):
    from kiro_dash.jsonl_parser import parse_jsonl_tools

    p = tmp_path / "x.jsonl"
    payload = (
        '{"kind":"AssistantMessage","data":{"content":[{"type":"toolUse","data":{"name":"read","toolUseId":"abc","input":{}}}]}}\n'
    )
    p.write_text(payload)

    out1 = parse_jsonl_tools(p)
    assert len(out1) == 1

    with patch("kiro_dash.jsonl_parser.open", wraps=open) as mock_open:
        out2 = parse_jsonl_tools(p)
        assert out2 == out1
        # Não deve abrir o .jsonl de novo no cache hit
        mock_open.assert_not_called()
```

- [ ] **Step 2: Rodar — falha**

```bash
pytest tests/test_cache_integration.py::test_jsonl_parser_uses_cache -v
```

- [ ] **Step 3: Implementar hook**

No topo de `jsonl_parser.py`:

```python
from kiro_dash.cache import jsonl_cache
```

Em `parse_jsonl_tools`:

```python
def parse_jsonl_tools(path: Path) -> list[ToolCall]:
    """Extrai tool calls do .jsonl. Cache mtime-based.

    `.jsonl` em sessão ativa pode estar incompleto/em-write — bypassamos
    cache se houver lockfile correspondente (mesma regra do parser).
    """
    if not path.exists():
        return []

    lock = path.with_suffix(".lock")
    if not lock.exists():
        cached = jsonl_cache().get(path)
        if cached is not None:
            return [ToolCall(**c) for c in cached]

    # leitura real (código existente)
    tools: list[ToolCall] = []
    # ... (preserva implementação atual)

    if not lock.exists():
        jsonl_cache().put(path, [t.__dict__ for t in tools])
    return tools
```

(Adapte conforme a implementação real de `parse_jsonl_tools` — preserve assinaturas e comportamento; só adicione o gate.)

- [ ] **Step 4: Rodar tudo**

```bash
pytest tests/ -v 2>&1 | tail -8
```

- [ ] **Step 5: Commit**

```bash
git add src/kiro_dash/jsonl_parser.py tests/test_cache_integration.py
git -c user.email='leonardo.menzani@gmail.com' -c user.name='mencoding' \
  commit -m "feat(jsonl_parser): cache de parse_jsonl_tools (bypass sessões ativas)"
```

---

### Task 4: Subgrupo `cache info/clear`

**Files:**
- Modify: `src/kiro_dash/cli.py`
- Modify: `tests/test_cli_projects_models_recent.py` ou novo `tests/test_cache_command.py`

- [ ] **Step 1: Escrever testes**

Criar `tests/test_cache_command.py`:

```python
from click.testing import CliRunner
from kiro_dash.cli import main


def test_cache_info_runs(tmp_path, monkeypatch):
    monkeypatch.setenv("XDG_CACHE_HOME", str(tmp_path))
    runner = CliRunner()
    result = runner.invoke(main, ["cache", "info"])
    assert result.exit_code == 0
    assert "sessions" in result.output.lower()
    assert "jsonl" in result.output.lower()


def test_cache_clear_runs(tmp_path, monkeypatch):
    monkeypatch.setenv("XDG_CACHE_HOME", str(tmp_path))
    runner = CliRunner()
    result = runner.invoke(main, ["cache", "clear"])
    assert result.exit_code == 0
    assert "limpo" in result.output.lower() or "removid" in result.output.lower()
```

- [ ] **Step 2: Rodar — falha**

```bash
pytest tests/test_cache_command.py -v
```

- [ ] **Step 3: Implementar**

Em `cli.py`:

```python
from kiro_dash.cache import cache_info, clear_cache, cache_dir_default


@main.group()
def cache() -> None:
    """Inspeção e limpeza do cache do parser."""


@cache.command("info")
def cache_info_cmd() -> None:
    """Mostra estatísticas do cache."""
    info = cache_info()
    table = Table(title="Cache", show_header=True)
    table.add_column("namespace")
    table.add_column("entries", justify="right")
    table.add_column("bytes", justify="right")
    for ns, stats in info.items():
        table.add_row(ns, str(stats["entries"]), str(stats["bytes"]))
    table.add_row("[dim]root[/dim]", "", str(cache_dir_default()))
    console.print(table)


@cache.command("clear")
def cache_clear_cmd() -> None:
    """Remove todas as entradas do cache."""
    out = clear_cache()
    total = sum(out.values())
    console.print(f"[green]Cache limpo:[/green] {total} entradas removidas {out}")
```

- [ ] **Step 4: Rodar — passa**

```bash
pytest tests/test_cache_command.py -v
```

- [ ] **Step 5: Commit**

```bash
git add src/kiro_dash/cli.py tests/test_cache_command.py
git -c user.email='leonardo.menzani@gmail.com' -c user.name='mencoding' \
  commit -m "feat(cli): subgrupo cache info/clear"
```

---

### Task 5: Benchmark + README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Bench manual (informativo)**

```bash
# Sem cache
KIRO_DASH_NO_CACHE=1 time kiro-dash today >/dev/null
# Com cache (1ª chamada — popular)
time kiro-dash today >/dev/null
# Com cache (2ª chamada — hit)
time kiro-dash today >/dev/null
```

Anotar resultados em commit message ou no README como evidência.

- [ ] **Step 2: Adicionar seção no README**

Antes da Licença:

```markdown
## Cache do parser

Leituras de `.json`/`.jsonl` são memoizadas em
`~/.cache/kiro-dash/` (ou `$XDG_CACHE_HOME/kiro-dash/`). Invalidação
automática por `mtime + size`. Sessões em curso (com `.lock`) bypassam
o cache para garantir frescor.

```bash
kiro-dash cache info     # estatísticas
kiro-dash cache clear    # limpa tudo
```

Para desligar pontualmente: `KIRO_DASH_NO_CACHE=1 kiro-dash today`.
```

- [ ] **Step 3: Commit**

```bash
git add README.md
git -c user.email='leonardo.menzani@gmail.com' -c user.name='mencoding' \
  commit -m "docs(cache): documentar invalidation e bypass"
```

---

## Self-Review Checklist

- [ ] Cache key inclui `path + mtime_ns + size`; qualquer mudança invalida
- [ ] Sessões ativas (`.lock`) **sempre** bypassam — correctness > speed
- [ ] `KIRO_DASH_NO_CACHE=1` desliga em qualquer caminho
- [ ] Cache é best-effort: erros de I/O não derrubam o parser
- [ ] `cache info` mostra entries/bytes por namespace
- [ ] `cache clear` é idempotente
- [ ] Round-trip `Session ↔ payload dict` é lossless

## Done When

- `pytest tests/ -v` → todos verdes
- Bench: 2ª chamada de `kiro-dash today` é claramente mais rápida (alvo: < 50% do tempo da 1ª em workspace com 17+ sessões)
- 5 commits no branch `feat/wave3-cache`
